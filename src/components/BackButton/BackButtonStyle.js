import styled from "styled-components";
import { HashLink } from "react-router-hash-link";

export const StyledBackBtn = styled(HashLink)`
  width: 50%;
  height: 35px;
  background-color: royalblue;
  color: white;
`;
